name: 低价机票监控

on:
  schedule:
    # 避开整点高峰；GitHub 定时任务可能有几分钟延迟。
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "只搜索，不推送"
        required: false
        default: false
        type: boolean
      test_notification:
        description: "只测试微信通知"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: flight-deal-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: .
    env:
      AMADEUS_CLIENT_ID: ${{ secrets.AMADEUS_CLIENT_ID }}
      AMADEUS_CLIENT_SECRET: ${{ secrets.AMADEUS_CLIENT_SECRET }}
      AMADEUS_ENVIRONMENT: production
      TRAVELPAYOUTS_TOKEN: ${{ secrets.TRAVELPAYOUTS_TOKEN }}
      PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
      PYTHONUNBUFFERED: "1"

    steps:
      - name: 下载代码
        uses: actions/checkout@v4

      - name: 安装 Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: 恢复去重状态
        uses: actions/cache/restore@v4
        with:
          path: state
          key: flight-monitor-state-${{ github.run_id }}
          restore-keys: |
            flight-monitor-state-

      - name: 安装依赖
        run: pip install -e .

      - name: 运行自动测试
        run: python -m unittest discover -s tests -v

      - name: 测试微信通知
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.test_notification }}
        run: python -m flight_monitor.app --test-notification

      - name: 执行监控
        if: ${{ !(github.event_name == 'workflow_dispatch' && inputs.test_notification) }}
        run: >-
          python -m flight_monitor.app
          ${{ (github.event_name == 'workflow_dispatch' && inputs.dry_run) && '--dry-run' || '' }}

      - name: 保存去重状态
        if: ${{ always() && hashFiles('state/monitor-state.json') != '' }}
        uses: actions/cache/save@v4
        with:
          path: state
          key: flight-monitor-state-${{ github.run_id }}
